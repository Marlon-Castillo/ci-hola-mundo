name: Alerta NTFY

on:
  push:
    branches:
      - main

jobs:
  notificar:
    runs-on: ubuntu-latest

    steps:
      - name: Enviar notificación a ntfy
        run: |
          curl -X POST https://ntfy.sh/devops-itla \
          -H "Title: Nuevo push en main" \
          -H "Priority: high" \
          -d "Se ha realizado un push al repositorio ci-hola-mundo en la rama main."
